// Minimal software renderer for the MiSTer framebuffer: off-screen canvas, blitted per frame.
#pragma once

#include <cstdio>
#include <cstring>
#include <cstdint>
#include <cstdlib>
#include <fcntl.h>
#include <unistd.h>
#include <sys/ioctl.h>
#include <sys/mman.h>
#include <linux/fb.h>

namespace font8x8 {

// Looked up per character range, so a miscounted table entry cannot shift the alphabet.
static const unsigned char UPPER[26][8] = {
    {0x18,0x3C,0x66,0x66,0x7E,0x66,0x66,0x00}, // A
    {0x7C,0x66,0x66,0x7C,0x66,0x66,0x7C,0x00}, // B
    {0x3C,0x66,0x60,0x60,0x60,0x66,0x3C,0x00}, // C
    {0x78,0x6C,0x66,0x66,0x66,0x6C,0x78,0x00}, // D
    {0x7E,0x60,0x60,0x7C,0x60,0x60,0x7E,0x00}, // E
    {0x7E,0x60,0x60,0x7C,0x60,0x60,0x60,0x00}, // F
    {0x3C,0x66,0x60,0x6E,0x66,0x66,0x3C,0x00}, // G
    {0x66,0x66,0x66,0x7E,0x66,0x66,0x66,0x00}, // H
    {0x3C,0x18,0x18,0x18,0x18,0x18,0x3C,0x00}, // I
    {0x1E,0x0C,0x0C,0x0C,0x0C,0x6C,0x38,0x00}, // J
    {0x66,0x6C,0x78,0x70,0x78,0x6C,0x66,0x00}, // K
    {0x60,0x60,0x60,0x60,0x60,0x60,0x7E,0x00}, // L
    {0x63,0x77,0x7F,0x6B,0x63,0x63,0x63,0x00}, // M
    {0x66,0x76,0x7E,0x7E,0x6E,0x66,0x66,0x00}, // N
    {0x3C,0x66,0x66,0x66,0x66,0x66,0x3C,0x00}, // O
    {0x7C,0x66,0x66,0x7C,0x60,0x60,0x60,0x00}, // P
    {0x3C,0x66,0x66,0x66,0x6E,0x3C,0x0E,0x00}, // Q
    {0x7C,0x66,0x66,0x7C,0x78,0x6C,0x66,0x00}, // R
    {0x3C,0x66,0x60,0x3C,0x06,0x66,0x3C,0x00}, // S
    {0x7E,0x18,0x18,0x18,0x18,0x18,0x18,0x00}, // T
    {0x66,0x66,0x66,0x66,0x66,0x66,0x3C,0x00}, // U
    {0x66,0x66,0x66,0x66,0x66,0x3C,0x18,0x00}, // V
    {0x63,0x63,0x63,0x6B,0x7F,0x77,0x63,0x00}, // W
    {0x66,0x66,0x3C,0x18,0x3C,0x66,0x66,0x00}, // X
    {0x66,0x66,0x66,0x3C,0x18,0x18,0x18,0x00}, // Y
    {0x7E,0x06,0x0C,0x18,0x30,0x60,0x7E,0x00}, // Z
};

static const unsigned char DIGIT[10][8] = {
    {0x3C,0x66,0x6E,0x7E,0x76,0x66,0x3C,0x00},
    {0x18,0x38,0x18,0x18,0x18,0x18,0x7E,0x00},
    {0x3C,0x66,0x06,0x0C,0x30,0x60,0x7E,0x00},
    {0x3C,0x66,0x06,0x1C,0x06,0x66,0x3C,0x00},
    {0x0C,0x1C,0x3C,0x6C,0x7E,0x0C,0x0C,0x00},
    {0x7E,0x60,0x7C,0x06,0x06,0x66,0x3C,0x00},
    {0x1C,0x30,0x60,0x7C,0x66,0x66,0x3C,0x00},
    {0x7E,0x06,0x0C,0x18,0x30,0x30,0x30,0x00},
    {0x3C,0x66,0x66,0x3C,0x66,0x66,0x3C,0x00},
    {0x3C,0x66,0x66,0x3E,0x06,0x0C,0x38,0x00},
};

static const unsigned char BLANK[8] = {0, 0, 0, 0, 0, 0, 0, 0};
static const unsigned char DASH[8]  = {0x00,0x00,0x00,0x7E,0x00,0x00,0x00,0x00};
static const unsigned char DOT[8]   = {0x00,0x00,0x00,0x00,0x00,0x18,0x18,0x00};
static const unsigned char COLON[8] = {0x00,0x18,0x18,0x00,0x18,0x18,0x00,0x00};
static const unsigned char BANG[8]  = {0x18,0x18,0x18,0x18,0x18,0x00,0x18,0x00};
static const unsigned char SLASH[8] = {0x06,0x0C,0x18,0x18,0x30,0x60,0x60,0x00};
static const unsigned char LPAR[8]  = {0x0C,0x18,0x30,0x30,0x30,0x18,0x0C,0x00};
static const unsigned char RPAR[8]  = {0x30,0x18,0x0C,0x0C,0x0C,0x18,0x30,0x00};
static const unsigned char COMMA[8] = {0x00,0x00,0x00,0x00,0x00,0x18,0x18,0x30};
static const unsigned char AMP[8]   = {0x38,0x6C,0x38,0x76,0x6E,0x6C,0x3A,0x00};

inline const unsigned char *glyph(char c) {
    if (c >= 'a' && c <= 'z') c = char(c - 32);
    if (c >= 'A' && c <= 'Z') return UPPER[c - 'A'];
    if (c >= '0' && c <= '9') return DIGIT[c - '0'];
    switch (c) {
    case '-': return DASH;
    case '.': return DOT;
    case ':': return COLON;
    case '!': return BANG;
    case '/': return SLASH;
    case '(': return LPAR;
    case ')': return RPAR;
    case ',': return COMMA;
    case '&': return AMP;
    default:  return BLANK;
    }
}

} // namespace font8x8

class Gfx {
public:
    ~Gfx() {
        std::free(back_);
        if (front_ && front_ != MAP_FAILED) munmap(front_, size_);
        if (fd_ >= 0) close(fd_);
    }

    bool open(const char *dev) {
        fd_ = ::open(dev, O_RDWR);
        if (fd_ < 0) { perror("open framebuffer"); return false; }
        if (ioctl(fd_, FBIOGET_VSCREENINFO, &var_) < 0) { perror("FBIOGET_VSCREENINFO"); return false; }
        if (ioctl(fd_, FBIOGET_FSCREENINFO, &fix_) < 0) { perror("FBIOGET_FSCREENINFO"); return false; }

        size_ = size_t(fix_.line_length) * var_.yres;
        front_ = (uint8_t *)mmap(nullptr, size_, PROT_READ | PROT_WRITE, MAP_SHARED, fd_, 0);
        if (front_ == MAP_FAILED) { perror("mmap framebuffer"); return false; }

        back_ = (uint8_t *)std::malloc(size_);
        if (!back_) { std::fprintf(stderr, "out of memory for back buffer\n"); return false; }
        return true;
    }

    int width()  const { return int(var_.xres); }
    int height() const { return int(var_.yres); }
    int bpp()    const { return int(var_.bits_per_pixel); }

    uint32_t rgb(uint8_t r, uint8_t g, uint8_t b) const {
        return (uint32_t(r) >> (8 - var_.red.length)) << var_.red.offset |
               (uint32_t(g) >> (8 - var_.green.length)) << var_.green.offset |
               (uint32_t(b) >> (8 - var_.blue.length)) << var_.blue.offset;
    }

    void clear(uint32_t color) { rect(0, 0, width(), height(), color); }

    void rect(int x, int y, int w, int h, uint32_t color) {
        if (x < 0) { w += x; x = 0; }
        if (y < 0) { h += y; y = 0; }
        if (x + w > width())  w = width() - x;
        if (y + h > height()) h = height() - y;
        if (w <= 0 || h <= 0) return;

        const int px = bpp() / 8;
        for (int j = 0; j < h; ++j) {
            uint8_t *row = back_ + size_t(y + j) * fix_.line_length + size_t(x) * px;
            if (bpp() == 32) {
                uint32_t *p = reinterpret_cast<uint32_t *>(row);
                for (int i = 0; i < w; ++i) p[i] = color;
            } else if (bpp() == 16) {
                uint16_t *p = reinterpret_cast<uint16_t *>(row);
                for (int i = 0; i < w; ++i) p[i] = uint16_t(color);
            }
        }
    }

    static int text_width(const char *s, int scale) {
        return int(std::strlen(s)) * 9 * scale;
    }

    void text(int x, int y, const char *s, int scale, uint32_t color) {
        for (int n = 0; s[n]; ++n) {
            const unsigned char *g = font8x8::glyph(s[n]);
            for (int row = 0; row < 8; ++row)
                for (int bit = 0; bit < 8; ++bit)
                    if (g[row] & (0x80 >> bit))
                        rect(x + (n * 9 + bit) * scale, y + row * scale, scale, scale, color);
        }
    }

    void text_centered(int y, const char *s, int scale, uint32_t color) {
        text((width() - text_width(s, scale)) / 2, y, s, scale, color);
    }

    void present() { std::memcpy(front_, back_, size_); }

private:
    int fd_ = -1;
    uint8_t *front_ = nullptr;
    uint8_t *back_ = nullptr;
    size_t size_ = 0;
    fb_var_screeninfo var_{};
    fb_fix_screeninfo fix_{};
};
